/** Workflow-safe constants (no process.env — Temporal sandbox has no process). */
export const AGENT_MAX_RETRIES = 3;
export const AGENT_MAX_REVIEW_ROUNDS = 2;
export const AGENT_MAX_PLAN_REVISIONS = 5;
