#!/usr/bin/env python3
"""OpenHands runner stub — real implementation requires OpenHands + OPENAI_API_KEY."""
import json
import sys

def main():
    print(json.dumps({
        "success": False,
        "filesChanged": [],
        "summary": "OpenHands runner stub: set OPENAI_API_KEY and configure OpenHands for real code changes."
    }))

if __name__ == "__main__":
    main()
